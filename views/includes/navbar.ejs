<style>
/* Navbar Styles */
header {
  background: #edffee;
  color: green;
  padding: 12px 40px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  position: sticky;
  top: 0;
  z-index: 1000;
  box-shadow: 0 2px 5px rgba(0,0,0,0.2);
}

.logo {
  font-size: 22px;
  font-weight: bold;
  display: flex;
  align-items: center;
  gap: 8px;
}

nav {
  display: flex;
  align-items: center;
  flex: 1;
  justify-content: center;
}

nav a {
  margin: 0 12px;
  padding: 6px 14px;
  color: black;
  text-decoration: none;
  font-weight: 500;
  transition: 0.3s;
  border-radius: 6px;
}

nav a:hover {
  background: rgba(168,201,168,0.15);
  transform: scale(1.05);
  color: #2e7d32;
}

.actions a {
  margin-left: 12px;
  padding: 6px 16px;
  border-radius: 20px;
  text-decoration: none;
  font-weight: bold;
  transition: 0.3s;
}

.actions a.signup {
  /* border: 2px solid black; */
  color: black;
}

.actions a.signup:hover {
  /* background: #f1f1f1;
  color: #2e7d32; */
  background: rgba(168,201,168,0.15);
  transform: scale(1.05);
  color: #2e7d32;
}

.actions a.signin {
  /* border: 2px solid black; */
  color: black;
}

.actions a.signin:hover {
  /* background: #fff;
  color: #2e7d32; */
  background: rgba(168,201,168,0.15);
  transform: scale(1.05);
  color: #2e7d32;
}

/* Responsive */
@media(max-width: 768px) {
  header {
    flex-direction: column;
    align-items: flex-start;
  }
  nav {
    margin: 10px 0;
  }
  .actions {
    margin-top: 8px;
    width: 100%;
    display: flex;
    justify-content: flex-start;
  }
}
</style>

<header>
  <!-- Logo -->
  <div class="logo">
    🌾 AgroConnect
  </div>

  <!-- Center Menu -->
  <nav>
    <a href="#home">Home</a>
    <a href="#features">Features</a>
    <a href="#about">About</a>
  </nav>

  <!-- Actions (Right Side) -->
  <div class="actions">
    <a href="#" class="signup">Signup</a>
    <a href="#" class="signin">Login</a>
    <a href="#" class="signin">Profile</a>
  </div>
</header>
